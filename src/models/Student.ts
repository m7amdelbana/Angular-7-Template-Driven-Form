import { User } from './User';

export class Student {
  _id: string;
  firstName: string;
  lastName: string;
  userId: string;
  user: User;
  gender: string;
  dateOfBirth: string;
  studentEmail: string;
}
